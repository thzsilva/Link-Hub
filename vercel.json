{
  "buildCommand": "cd artifacts/void && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "artifacts/void/dist/public",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://link-hub-production.up.railway.app/api/:path*"
    }
  ]
}
